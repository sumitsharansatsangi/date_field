name: date_field
description: A widget in the form of a field that lets people choose a date, a time or both.
version: 3.0.1
homepage: "https://github.com/GaspardMerten/date_field"
publish_to: "none"

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl:
    git:
      url: https://github.com/dart-lang/intl.git
